import frameself.format.*;

rule "Init Effectors"
	when
    then
 	   insert(new Effector("KnowledgeBaseManager"));  
 	   insert(new Effector("PhillipsLamp"));  
		insert(new Effector("TableauLED"));  
end
