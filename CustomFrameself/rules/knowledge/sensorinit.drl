import frameself.format.*;

rule "Init Sensors"
	when
    then
    	insert(new Sensor("TemperatureSensor"));
    	insert(new Sensor("WeightSensor"));
    	insert(new Sensor("LampStateSensor"));
end